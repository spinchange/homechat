# Dependencies
node_modules/

# User data — local only, never commit
messages.ndjson
rooms.json

# Uploaded images
public/uploads/*
!public/uploads/.gitkeep

# Server logs
logs/*
!logs/.gitkeep

# Environment / secrets
.env

# Claude memory — family-specific, local only
claude-context.txt
